name: .NET CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8.0.x

    - name: Restore dependencies
      run: dotnet restore oop-s2-2-mvc-77928.sln

    - name: Build solution
      run: dotnet build oop-s2-2-mvc-77928.sln --no-restore --configuration Release

    - name: Run tests
      run: dotnet test oop-s2-2-mvc-77928.sln --no-build --configuration Release